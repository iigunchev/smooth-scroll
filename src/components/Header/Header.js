import React, { useEffect, useRef, useState } from "react";

import gsap from "gsap";
// import SplitText from '../../utils/Split3.min';

import "./style.scss";

const Header = () => {
  return <section className="header-container">Header</section>;
};

export default Header;
